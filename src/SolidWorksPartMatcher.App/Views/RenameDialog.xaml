<Window x:Class="SolidWorksPartMatcher.App.Views.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Group" Height="175" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="New canonical name for this group:" Margin="0,0,0,6"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Height="28" Padding="4,2"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Rename" Width="80" IsDefault="True" Click="OkClick"
                    Padding="8,4" Margin="0,0,6,0"/>
            <Button Content="Cancel" Width="80" IsCancel="True" Padding="8,4"/>
        </StackPanel>
    </Grid>
</Window>
